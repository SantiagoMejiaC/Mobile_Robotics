launch:
#include another launch file
- include:
     file: "$(find-pkg-share grp_jarvis)/launch/challenge-1.launch.py"

#- node:
 #   pkg: "rviz2"
  #  exec: "rviz2"
   # name: "rviz2"
    
- node:
    pkg: "grp_jarvis"
    exec: "move.py"
    name: "scan_interpreter"

#- node:
 #   pkg: "tbot_node"
  #  exec: "multiplexer.py"
   # name: "multiplexer"

- executable:
    cmd: gnome-terminal --tab -e 'ros2 run  teleop_twist_keyboard teleop_twist_keyboard cmd_vel:= /multi/cmd_teleop'

- executable:
    cmd: gnome-terminal --tab -e 'ros2 run  tbot_node multiplexer'

